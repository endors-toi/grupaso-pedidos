import 'package:flutter/material.dart';

class PlaceHolderUsuario extends StatelessWidget {
  const PlaceHolderUsuario({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Usuarios"));
  }
}
