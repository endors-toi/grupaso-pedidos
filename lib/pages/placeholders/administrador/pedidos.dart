import 'package:flutter/material.dart';

class PlaceHolderPedidos extends StatelessWidget {
  const PlaceHolderPedidos({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Pedidos"));
  }
}
