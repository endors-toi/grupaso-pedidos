import 'package:flutter/material.dart';

class PlaceHolderProductos extends StatelessWidget {
  const PlaceHolderProductos({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Productos"));
  }
}
